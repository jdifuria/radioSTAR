<nav class="space-theme navbar navbar-default navbar-fixed-top navbar-inverse">
<div class="container-fluid">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <ul class="nav navbar-nav">
        <li>
          <a class="navbar-brand" href="/" data-toggle="tooltip" data-placement="bottom" title="Statistical Tracking and Automated Review">
            <% if user_signed_in? %>
              <%= Station.find(current_user.station_id).station_name %>
            <% else %>
              RadioSTAR
            <% end %>
          </a>
        </li>
    </ul>
  </div>
  <div class="collapse navbar-collapse" id="mainMenu">
    <ul class="nav navbar-nav">
      <li class="<%= is_active(root_url) %>">
        <%= link_to "Home", root_url, :remote => true %>
      </li>
      <% if user_signed_in? %>
        <% if current_user.admin == true %>
          <li>
            <%= link_to "Manage", root_url, :remote => true %>
          </li>
        <% end %>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li>
          <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
        </li>
      <% else %>
        <li class="<%= is_active(new_user_session_path) %>">
          <%= link_to "Login", new_user_session_path %>
        </li>
        <li class="<%= is_active(new_user_registration_path) %>">
          <%= link_to "Sign Up", new_user_registration_path %>
        </li>
      <% end %>
    </ul>

  </div>
</div>
</nav>
<br><br><br>
