<%= form_for(@user_show) do |f| %>
  <% if @user_show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_show.errors.count, "error") %> prohibited this user_show from being saved:</h2>

      <ul>
      <% @user_show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :show_id %><br>
    <%= f.number_field :show_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
